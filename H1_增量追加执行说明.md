# 2025 上半年数据增量追加执行说明

## 目标

在不影响现有下半年拆分结果的前提下，将上半年数据追加到已拆分的公司目录，再按全年进行匹配与分析。

## 已完成（2026-03-06）

- ✅ 上半年原始数据放入 `InPut_H1_RAW`
- ✅ `cleanup_h1_raw.sh` 清理空目录、已解压 ZIP
- ✅ `run_h1_append_split.sh` 完成：展平 → 备份 → 追加拆分
- ✅ 43 个公司目录已含 H1+H2 全年数据
- ⏳ 待执行：`run_full_year_pipeline.sh` 三单匹配与差异分析

## 脚本说明

| 脚本 | 说明 |
|------|------|
| `flatten_h1_to_root.sh` | 将子目录中 TXT 符号链接到根目录，供 split 读取 |
| `cleanup_h1_raw.sh` | 删除空文件夹、已解压的 ZIP（可选，手动执行） |
| `run_h1_append_split.sh` | 展平 → 备份 InPut → split_by_vkorg --append |
| `run_full_year_pipeline.sh` | run_all_by_company + aggregate_difference_summary |

## 操作顺序（供下次或其他环境参考）

1. 将上半年原始 TXT 放入 `InPut_H1_RAW`
2. （可选）清理：`bash cleanup_h1_raw.sh`
3. 追加拆分：`bash run_h1_append_split.sh`
4. 拆分完成后执行全年流程：`bash run_full_year_pipeline.sh 1 4010`

## 说明

- `run_h1_append_split.sh` 会先备份当前 `InPut`，再以 `--append` 方式追加拆分上半年数据。
- `run_full_year_pipeline.sh` 会执行：`run_all_by_company.py --start <code> --workers <n>` + `aggregate_difference_summary.py`

